const styles = (theme) => ({
  textfield: {
    width: '100%',
  },
  resize1: {
    fontSize: 20,
  },
  resize2: {
    fontSize: 15,
  },
  select: {
    // margin: theme.spacing(1),
    // width: '100%',
  },
});

export default styles;
